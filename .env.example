# Example .env for local Claw demo
# Copy to .env and fill values to enable live local demo against your clawdbot

# Local Claw endpoint (optional). Example from your dashboard:
# http://127.0.0.1:18789/heartbeat
CLAW_ENDPOINT=

# Auth token for Claw (copy token from your dashboard URL)
CLAW_AUTH_TOKEN=

# When empty, the demo runs in safe simulated mode and performs no network calls.